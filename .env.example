# ContentForge - Environment Variables Template
# Copy this file to .env.local and fill in your values

# =============================================================================
# Client Configuration (exposed to browser via Vite)
# =============================================================================

VITE_APP_NAME=ContentForge
VITE_APP_URL=http://localhost:5173

# =============================================================================
# Server Configuration
# =============================================================================

# Port for the Express server
SERVER_PORT=3001

# JWT secret for authentication tokens
# IMPORTANT: Generate a secure random string for production (min 32 chars)
# Example: openssl rand -base64 32
JWT_SECRET=

# PostgreSQL connection string
DATABASE_URL=postgres://user:password@localhost:5432/contentforge

# Allowed CORS origins (comma-separated, for production)
# ALLOWED_ORIGINS=https://yourdomain.com,https://app.yourdomain.com

# =============================================================================
# Initial Setup (remove after first run)
# =============================================================================

# Password for the initial admin user (admin@contentforge.com)
# Must be at least 12 characters with mixed case, numbers, and symbols
# Remove this variable after the admin user is created
ADMIN_INITIAL_PASSWORD=

# =============================================================================
# Cloudflare R2 Storage (optional)
# =============================================================================

# If not configured, files are stored locally in ./uploads
# R2_ENDPOINT=https://<account-id>.r2.cloudflarestorage.com
# R2_ACCESS_KEY_ID=
# R2_SECRET_ACCESS_KEY=
# R2_BUCKET_NAME=contentforge-files
